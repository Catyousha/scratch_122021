import 'package:flutter/material.dart';

void main() {
  runApp(const Scratch122021());
}

class Scratch122021 extends StatelessWidget {
  const Scratch122021({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      title: 'Scratch 122021',
      home: Center(
        child: Text("P"),
      ),
    );
  }
}